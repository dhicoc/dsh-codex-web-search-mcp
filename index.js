/**
 * Runtime marker for dshmarket validation.
 *
 * This package is a configuration-only dsh bundle; its actual integration is
 * declared in cordis.patch.yml. The side-effect-free ESM entry makes that
 * contract explicit for package consumers and marketplace validation.
 */
export {}
